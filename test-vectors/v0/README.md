# v0 Composable Skills Vectors

These fixtures cover the `spec/v0.md` contracts:

- `skill-manifest/` for skill manifest parsing checks.
- `pipeline/` for DAG + edge mapping checks.
- `event-chain/` for deterministic event hashing and hash-chain validation.
- `cap-resolution/` for capability intersection behavior.
